---
const { align = "" } = Astro.props;
---

<div class:list={["mt-16", align === "center" && "text-center", align === "end" && "text-end"]}>
  <h1 class="text-2xl lg:text-3xl font-bold lg:tracking-tight text-center text-black dark:text-gray-400">
    <slot name="title">Title</slot>
  </h1>
  <p class="text-lg text-slate-600">
    <slot name="desc">Some description goes here</slot>
  </p>
</div>
        